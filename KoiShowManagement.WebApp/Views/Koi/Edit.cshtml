@model KoiShowManagement.Repositories.Entities.Koi

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chỉnh Sửa Cá Koi</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/admin-lte@3.2.0/dist/css/adminlte.min.css">
</head>
<body>
    <div class="wrapper">
        @await Html.PartialAsync("_Navbar")

        <div class="content-wrapper">
            <div class="content-header">
                <h1>Chỉnh Sửa Cá Koi</h1>
            </div>

            <div class="content">
                <div class="container-fluid">
                    <div class="card">
                        <div class="card-header">
                            <h3 class="card-title">Chỉnh Sửa Cá Koi</h3>
                        </div>
                        <div class="card-body">
                            <form method="post" action="@Url.Action("Edit", "Koi", new { id = Model.Id })">
                                <div class="form-group">
                                    <label for="Name">Tên Cá</label>
                                    <input type="text" class="form-control" id="Name" name="Name" value="@Model.Name" required />
                                </div>
                                <div class="form-group">
                                    <label for="Gender">Giới tính</label>
                                    <input type="text" class="form-control" id="Gender" name="Gender" value="@Model.Gender" required />
                                </div>
                                <div class="form-group">
                                    <label for="Age">Tuổi</label>
                                    <input type="number" class="form-control" id="Age" name="Age" value="@Model.Age" required />
                                </div>
                                <button type="submit" class="btn btn-primary">Lưu</button>
                                <a href="@Url.Action("Index", "Koi")" class="btn btn-secondary">Hủy</a>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        @await Html.PartialAsync("_Footer")
    </div>

    <script src="https://cdn.jsdelivr.net/npm/jquery@3.6.0/dist/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/admin-lte@3.2.0/dist/js/adminlte.min.js"></script>
</body>
</html>
